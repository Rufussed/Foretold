{
  "name": "wizard-platform",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "play": "node scripts/play.mjs",
    "start": "node server.js",
    "postinstall": "npm --prefix backend install && npm --prefix backend run build && vite build"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3",
    "typescript": "~6.0.2"
  },
  "dependencies": {
    "@types/three": "^0.186.0",
    "qrcode": "^1.5.4",
    "three": "^0.186.0",
    "tsx": "^4.23.13",
    "vite": "^8.2.2"
  }
}
